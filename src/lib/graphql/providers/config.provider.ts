/**
 * GraphQl configuration injection token.
 *
 * TODO: Use "new InjectionToken<GraphQlConfig>('GRAPHQL_CONFIG')" when this is a standalone library.
 */
export const GRAPHQL_CONFIG = 'GRAPHQL_CONFIG';

/**
 * GraphQl configuration injection token for server.
 *
 * TODO: Use "new InjectionToken<Partial<GraphQlConfig>>('SERVER_GRAPHQL_CONFIG')" when this is a standalone library.
 */
export const SERVER_GRAPHQL_CONFIG = 'SERVER_GRAPHQL_CONFIG';
